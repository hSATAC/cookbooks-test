# This file is managed by Chef for <%= @node[:hostname] %>
# Do NOT modify this file directly.
# Or remember apply changes to Chef configs.

check process openapply_<%= @environment %>_memcached
  with pidfile /var/run/memcached.pid
  start program = "/etc/init.d/memcached start"
  stop program =  "/etc/init.d/memcached stop"
  if does not exist 2 cycles then restart
  if 3 restarts within 8 cycles then timeout
